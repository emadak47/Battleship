#ifndef STREAMOUT_H
#define STREAMOUT_H

#include <string>
#include "Node.h"

using namespace std;

void print_output(string outputfile, Node * head, char grid[][10+1]);
void print_list(Node *head);

#endif
